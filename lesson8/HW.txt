1.Продумать, где можно применить замыкания для практикума из седьмого урока.

-спрятать переменные внутри замыканий чтобы их не могли менять напрямую через консоль, но с приходом приватных свойств объектов наверное это не очень актуально.
-в этой же логике можно функцию движения переписать убрав все переменные внуть и возвращать 4 замыкания(движение в право, лево,вверх и низ)


2.Не выполняя код, ответить, что выведет браузер и почему:
 
if (!("a" in window)) {
    var a = 1;
}
alert(a);// выведет 1, при условии если a не была указана раньше в качестве глобальной переменной. Сначала условие проверяет существует ли глобальная переменная с именем a, если нет то создаёт. 


var b = function a(x) {
    x && a(--x);
};
alert(a);//undefined, a это функция которая ничего не возвращает
//проверил оказалось выводит ошибку, я не учёл что а вызывается именно как переменная, а не как функция. Но даже если вызвать как функцию то выдаёт ошибку, т.к. видимо так нельзя объявлять функции это либо должна быть анонимная функция присвоенная b, либо именованная функция но объявленная ранее.


function a(x) {
    return x * 2;
}
var a;
alert(a);//undefined, переменная а определена но её значение не указано.
//проверил выводит тескт функции непонятно почему... Видимо при объявлении функции она записывается в глобальную переменную с таким же именем чтобы была возможность присвоить другой переменной или передать как аргумент функции, при этом var видимо имеет меньший приоритет т.к. объявляет переменную только внутри функции, поэтому алерт выводит глобальную переменную.




function b(x, y, a) {
    arguments[2] = 10;
    alert(a);
}
b(1, 2, 3);// думаю выведет 3 т.к. никогда не видел чтобы через arguments обращались к аргументам функции
//выдало 10 узнал новый синтаксис, буду иметь в виду, но пока не очень знаю зачем мне это может пригодиться, разве что узнать кол-во переменных одной командой.


*
function a() {
    alert(this);
}
a.call(null);// должно вывести null, т.к. метод call явно указывает контекст(this).
//ну почти правильно, только видимо из за того что в null это пустой объект то выводит [object Window], но если вместо this прописать null в функции то выводить null, что странно...
